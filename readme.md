# Dashboard de Gestión de Empleados

## 📝 Diario de Commits

* **feat: estructura semantica inicial del formulario de login en index.html**
  * Se crea el archivo `index.html` con el formulario de acceso administrativo usando etiquetas semánticas.

* **cs: modular CSS**
  * Se crean los módulos `global.css` y `login.css` dentro de `src/css` y se enlazan en la raíz mediante `@import` en `style.css`.